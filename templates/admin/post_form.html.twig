{% extends 'admin/base.html.twig' %}
{% set active_tab = 'posts' %}

{% block title %}{{ title }} - Admin{% endblock %}

{% block admin_content %}
<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="card shadow-sm border-0">
            <div class="card-header bg-success text-white">
                <h4 class="mb-0">📝 {{ title }}</h4>
            </div>
            <div class="card-body p-4">
                {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                    
                    <div class="form-group">
                        <label for="{{ form.title.vars.id }}" class="font-weight-bold">
                            <i class="mr-1">📌</i> Titre de l'article
                        </label>
                        {{ form_widget(form.title, {'attr': {'class': 'form-control form-control-lg', 'placeholder': 'Entrez un titre accrocheur...'}}) }}
                        {{ form_errors(form.title) }}
                    </div>
                    
                    <div class="form-group">
                        <label for="{{ form.content.vars.id }}" class="font-weight-bold">
                            <i class="mr-1">📄</i> Contenu
                        </label>
                        {{ form_widget(form.content, {'attr': {'class': 'form-control', 'rows': '12', 'placeholder': 'Rédigez votre article ici...'}}) }}
                        {{ form_errors(form.content) }}
                        <small class="form-text text-muted">Astuce : Utilisez des paragraphes clairs pour une meilleure lisibilité.</small>
                    </div>
                    
                    <div class="form-group">
                        <label for="{{ form.publishedAt.vars.id }}" class="font-weight-bold">
                            <i class="mr-1">📅</i> Date de publication
                        </label>
                        {{ form_widget(form.publishedAt, {'attr': {'class': 'form-control'}}) }}
                        {{ form_errors(form.publishedAt) }}
                    </div>
                    
                    <hr class="my-4">
                    
                    <div class="d-flex justify-content-between align-items-center">
                        <a href="{{ path('admin_posts') }}" class="btn btn-outline-secondary">
                            ← Annuler
                        </a>
                        <button type="submit" class="btn btn-success btn-lg px-5">
                            ✓ Enregistrer l'article
                        </button>
                    </div>
                    
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
