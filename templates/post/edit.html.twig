{% extends 'base.html.twig' %}

{% block title %}Edit Post{% endblock %}

{% block body %}
    <div class="row justify-content-start">
        <div>
            <h1 class="mb-3">Edit {{ post.title }}</h1>

            <div class="card shadow-sm border-0 mb-4">
                <div class="card-body p-4">
                    {{ include('post/_form.html.twig', {'button_label': 'Update'}) }}
                </div>
            </div>

            <div class="d-flex justify-content-between align-items-center">
                <a href="{{ path('app_post_show', {'slug': post.slug}) }}" class="btn btn-link text-decoration-none ps-0"><i class="bi bi-arrow-left me-1"></i> Retour à l'article</a>
                {{ include('post/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
